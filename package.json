{
  "name": "intro-to-js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t alpine-js .",
    "docker:run": "docker run --init --rm -p 3000:3000 alpine-js",
    "docker:ash": "docker run --init --rm -p 3000:3000 alpine-js sh",
    "docker:inspect": "docker inspect alpine-js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^19.1.1",
    "hapi-pino": "^6.3.0"
  }
}
